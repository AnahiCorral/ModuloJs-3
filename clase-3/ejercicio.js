/* 1. Tabla de multiplicar de un número ingresado por el usuario
const numero = parseInt(prompt("Ingresa un número para ver su tabla de multiplicar:"));
console.log(`Tabla de multiplicar del ${numero}:`);
for (let i = 1; i <= 10; i++) {
    console.log(`${numero} x ${i} = ${numero * i}`);
}
*/


/*2. Contar los dígitos de un número*/

/*4. Encontrar el número mayor en un arreglo
const numeros = [12, 45, 78, 23, 89, 34, 67];
let maximo = numeros[0];
for (let i = 1; i < numeros.length; i++) {
    if (numeros[i] > maximo) {
        maximo = numeros[i];
    }
}
console.log(`El número mayor en el arreglo es ${maximo}.`);9*/




